// fibonacci index calculator
// input n on stack and returns n+2 in the fib sequence

include "std.pang"

clear

macro fib
    push 0
    push 1 over
    
    while 0 < do
        push 1- over
        dup over+ over
    end drop drop
end

push 11
fib printi
