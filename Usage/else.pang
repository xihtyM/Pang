include 'std.pang'

2 > dup if do
    // must purge so that 
    "Hello, World!\n" puts purge
end else do
    // no need to purge anymore,
    // however it is recommended if you do not need it later
    "Try adding 1 more argument...\n" puts purge
end
